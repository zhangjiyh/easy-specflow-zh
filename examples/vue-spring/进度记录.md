# 进度记录

## 1. 当前看板

> 说明：`BLOCKED` 未清空，或存在待补验证项时，任务不得标记为 `done`。

- TODO：refresh token 独立验证
- DOING：无
- DONE：Step 1、Step 2、Step 3
- BLOCKED：refresh token 场景缺少稳定联调环境

## 2. 执行记录

### 记录 1

- 对应步骤编号：1
- 执行时间：2026-03-29 15:10 +08:00
- 当前阶段：execute
- 本次目标：完成后端登录接口与 JWT 鉴权拦截器改造。
- 实际修改内容：登录接口返回 token，拦截器校验 `Authorization` 头，白名单保留登录接口。
- 影响文件：`AuthController.java`、`AuthService.java`、`JwtTokenInterceptor.java`、`SecurityConfig.java`
- 验证命令：`./mvnw test`
- 验证结果：后端测试通过，未授权访问返回 401。
- 是否通过：是
- 问题与阻塞：refresh token 逻辑暂未纳入后端测试。
- 下一步动作：接入前端登录流程和请求拦截器。

### 记录 2

- 对应步骤编号：2
- 执行时间：2026-03-29 16:00 +08:00
- 当前阶段：execute
- 本次目标：接入前端登录页、用户 store 和请求拦截器。
- 实际修改内容：登录成功后保存 token，请求头自动注入 token，401 时跳回登录页。
- 影响文件：`index.vue`、`user.ts`、`http.ts`、`router/index.ts`
- 验证命令：手动验证登录成功、访问受保护页面、移除 token 后重试访问
- 验证结果：登录和未授权跳转行为正常。
- 是否通过：是
- 问题与阻塞：记住我续期链路仍缺少稳定复现环境。
- 下一步动作：整理联调结果并记录待补验证项。

### 记录 3

- 对应步骤编号：3
- 执行时间：2026-03-29 16:40 +08:00
- 当前阶段：execute
- 本次目标：同步联调说明并准备验收。
- 实际修改内容：汇总前后端联调结果，补充待补验证项说明。
- 影响文件：联调说明、`验证记录.md`
- 验证命令：`pnpm build`
- 验证结果：前端构建通过，联调主链路验证通过。
- 是否通过：待补验证
- 问题与阻塞：refresh token 续期场景待补验证。
- 下一步动作：进入 `verify` 汇总当前证据，并明确不能标记 `done`。

## 3. 计划同步状态

- Step 1：已完成并回填记录 1
- Step 2：已完成并回填记录 2
- Step 3：已完成并回填记录 3，但存在待补验证项

## 4. 决策与备注

- 当前主链路可用，但 refresh token 场景没有足够证据支撑完全通过。

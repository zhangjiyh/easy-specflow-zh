# Vue + Spring 复杂案例

这个目录提供一个更复杂的前后端联动样例，帮助你看到 `repo-map` 与验证证据在跨模块任务里的价值。

任务标题：

`登录与鉴权重构`

这个样例有三个重点：

1. `代码库地图.md` 会同时标出前端登录流程、请求封装、后端鉴权入口和配置文件。
2. `验证记录.md` 会记录接口联调、未授权访问、token 失效等验证证据，而不是只写“已验证”。
3. `验收记录.md` 最终结论是“有条件通过”，对应 `state.json.status=active`，用来示范“有条件通过不等于 done”。

# 任务说明

## 1. 基本信息

- 任务 ID：T20260329-001
- 任务标题：任务看板筛选条件统一为 criteria 对象
- 文档所属阶段：spec
- 负责人 / 当前 agent：Codex
- 创建时间：2026-03-29 09:00 +08:00
- 下一阶段目标：补全 `代码库地图.md`，确认真实代码边界后再进入 `plan`

## 2. 背景

- 业务背景：任务看板页面当前把筛选条件分散在页面 state、请求参数拼接函数和 URL query 中，维护成本高。
- 现状问题：前端页面和 API 适配层分别维护一套参数结构，新增筛选项时容易漏改。
- 触发原因：团队计划把筛选条件统一收敛为 `criteria` 对象，减少页面与请求适配之间的重复转换。

## 3. 目标

- 目标 1：页面、store 和 API 请求统一使用 `criteria` 对象。
- 目标 2：保持现有筛选能力不回退。
- 目标 3：让回归验证能够覆盖 URL 恢复、筛选提交和清空条件三个关键场景。

## 4. 范围

### 本次范围

- 任务看板页筛选状态模型
- 请求参数适配函数
- 筛选说明文档

### 非目标

- 不改动任务列表渲染逻辑
- 不新增后端筛选字段

## 5. 约束与边界

- 禁止无关重构：不顺手改 UI 样式或任务列表组件结构。
- 禁止未授权依赖升级：保持现有依赖版本不变。
- 禁止擅自修改部署 / 基础设施：不改构建链路与 CI。
- 其他限制：筛选行为要与旧参数保持兼容，避免后端接口联调被打断。

## 6. 验收标准

> 说明：这些勾选项要在 `verify` / `accept` 阶段按真实结果回填，不能在任务结束时仍全部保持未勾选；结论应能在 `验证记录.md` 和 `验收记录.md` 中找到对应依据。

- [x] 页面筛选提交后，请求参数统一以 `criteria` 结构发送。
- [x] URL 恢复筛选条件与页面回显保持一致。
- [x] 清空筛选条件后，不再残留旧 query 字段。

## 7. 风险与待确认事项

- 风险：旧链接可能还携带散落 query 字段，需要兼容解析。
- 待确认：无。
- 备注：此任务已完成验收，可作为完整闭环案例参考。

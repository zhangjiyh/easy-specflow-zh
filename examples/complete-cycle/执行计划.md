# 执行计划

## 1. 计划概览

- 任务 ID：T20260329-001
- 文档所属阶段：plan
- 计划负责人 / 当前 agent：Codex
- 前置输入：`任务说明.md`、`代码库地图.md`
- 总体策略：先统一筛选状态模型，再改请求适配与页面调用，最后补文档和回归验证。

## 2. 步骤列表

> 说明：`execute` 阶段默认连续推进剩余步骤；每完成一步都要立即同步当前状态，但不要因为单步完成就默认中断整轮执行。只要仍有未完成步骤，就不得进入 `verify` / `accept`。

- [x] Step 1：统一筛选状态模型与请求适配字段
- [x] Step 2：更新任务看板页面调用和 URL 恢复逻辑
- [x] Step 3：补充说明文档并完成回归验证

## 3. 结构化步骤说明

### Step 1：统一筛选状态模型与请求适配字段

- 步骤编号：1
- 步骤目标：让 store 与 API 层统一使用 `criteria` 对象。
- 修改范围 / 涉及文件：`src/stores/taskBoardFilters.ts`、`src/api/taskBoard.ts`
- 执行动作：重构筛选状态接口，删除散落 query 字段，统一请求适配函数入参。
- 验证方式：运行单元测试并检查构造出的请求参数结构。
- 完成标准：页面提交筛选时，请求 payload 只出现 `criteria` 对象。
- 当前状态：已完成

### Step 2：更新任务看板页面调用和 URL 恢复逻辑

- 步骤编号：2
- 步骤目标：让页面提交、清空和 URL 恢复逻辑都使用统一模型。
- 修改范围 / 涉及文件：`src/pages/task-board/index.tsx`、`src/pages/task-board/hooks/useTaskBoardQuery.ts`
- 执行动作：替换旧 query 字段读取逻辑，统一从 `criteria` 对象初始化和提交。
- 验证方式：手动验证筛选提交、刷新后回显和清空条件。
- 完成标准：三个核心交互都不再依赖旧散落 query 字段。
- 当前状态：已完成

### Step 3：补充说明文档并完成回归验证

- 步骤编号：3
- 步骤目标：把参数结构变化和验证结果同步到文档。
- 修改范围 / 涉及文件：`docs/task-board/filters.md`
- 执行动作：更新文档中的参数示例，并汇总验证结论。
- 验证方式：运行 `pnpm lint`，人工复核文档示例与实际请求一致。
- 完成标准：文档、执行记录和验证记录三者保持一致。
- 当前状态：已完成

## 4. 风险与依赖

- 风险 1：旧链接兼容解析处理不当会影响历史分享链接。
- 风险 2：页面与请求适配两侧若同时保留旧字段，容易让验证失真。
- 外部依赖：无。
- 回退策略：保留旧字段解析兜底，必要时回退到旧 query 适配函数。

## 5. 阶段切换条件

- 进入 execute 前需要满足：
  - `任务说明.md` 已明确目标、范围、约束和验收标准。
  - `代码库地图.md` 已识别关键目录、关键入口文件、直接相关文件和潜在影响面。
  - 每个已承诺步骤都已写明验证方式与完成标准。
- 进入 verify 前需要满足：
  - 关键步骤已完成。
  - `进度记录.md` 已记录每个已完成步骤的执行结果。
  - `验证记录.md` 可以据此汇总验证证据。

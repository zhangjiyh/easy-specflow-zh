# 代码库地图

## 1. 基本信息

- 任务 ID：T20260329-001
- 任务标题：任务看板筛选条件统一为 criteria 对象
- 文档所属阶段：repo-map
- 负责人 / 当前 agent：Codex
- 创建时间：2026-03-29 09:20 +08:00

## 2. 任务目标概述

- 本次要解决的问题：统一页面筛选状态与请求参数结构，减少多处重复映射。
- 与代码库定位直接相关的目标：先找出页面入口、筛选 store、请求适配函数和文档位置。

## 3. 关键目录

| 目录 / 模块 | 作用 | 本次关注点 |
| --- | --- | --- |
| `src/pages/task-board/` | 页面入口与交互 | 筛选提交、清空、URL 回填 |
| `src/stores/` | 页面状态管理 | 筛选条件模型 |
| `src/api/` | 请求封装 | `criteria` 参数适配 |
| `docs/task-board/` | 页面说明文档 | 参数结构说明同步 |

## 4. 关键入口文件

- `src/pages/task-board/index.tsx`
- `src/stores/taskBoardFilters.ts`
- `src/api/taskBoard.ts`

## 5. 直接相关文件

- `src/pages/task-board/index.tsx`
- `src/stores/taskBoardFilters.ts`
- `src/api/taskBoard.ts`
- `docs/task-board/filters.md`

## 6. 潜在影响文件

- `src/utils/query.ts`
- `src/pages/task-board/hooks/useTaskBoardQuery.ts`

## 7. 搜索关键词 / 路径提示

- 关键词 1：`taskBoardFilters`
- 关键词 2：`query.status`
- 路径提示：从 `task-board` 页面入口一路追到请求适配层

## 8. 依赖关系简述

- 上下游依赖：页面提交筛选条件后，store 负责管理当前值，API 层负责把结构转换为接口参数。
- 配置 / 接口 / 数据流关系：URL query -> 页面初始化 -> store -> API request -> 文档说明。

## 9. 风险点 / 不确定项

- 风险 1：旧 query 字段兼容处理不当会导致历史链接回显失败。
- 不确定项 1：`useTaskBoardQuery` 是否被其他页面复用，需要回归。

## 10. 本次建议优先查看文件

1. `src/pages/task-board/index.tsx`
2. `src/stores/taskBoardFilters.ts`
3. `src/api/taskBoard.ts`
